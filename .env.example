# AKUA Stack Environment Variables
# Copy to .env and fill in actual values

# BSV Blockchain (required for mainnet publishing)
BSV_ADDRESS=your_bsv_address_here
BSV_PRIVATE_KEY=your_private_key_here
BSV_NETWORK=mainnet

# M2 Security & Rate Limiting (optional but recommended)
# If PUBLISHER_AUTH_TOKEN is set, all requests to /publish must include:
# Authorization: Bearer <token>
PUBLISHER_AUTH_TOKEN=your-secret-token-here

# Rate limiting (requests per minute)
RATE_LIMIT_PER_MIN=100

# Fee controls (satoshis)
MAX_FEE_SATS=10000
MIN_BALANCE_SATS=1000000

# Test mode (set to 1 for tests, 0 for production)
# When TEST_PUBLISHER_STUB=1:
# - Generates deterministic fake txids (no blockchain API calls)
# - Prevents spending real BSV during test runs
TEST_PUBLISHER_STUB=0

# RabbitMQ Management API (for integration tests)
RABBIT_HTTP=http://rabbitmq:15672
RABBIT_USER=akua
RABBIT_PASS=akua_pass
RABBIT_VHOST=/
